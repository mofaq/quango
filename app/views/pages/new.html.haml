- rendering_start = Time.now
- is_mobile = params[:mobile]


-content_for :main do
  #main-index

  - if is_mobile || current_group.group_type == "mobile"
    = render :partial => "/pages/new_mobile"
  - else
    = render :partial => "/pages/new"


