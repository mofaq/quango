
.clear

-if current_user
  -if current_user.owner_of?(current_group)
    .footer-mobile{:style=>"width:480px;padding:16px;margin:0 16px 1px 0;font-size:0.8em;border-top:0px solid #{current_group.secondary};border-bottom:1px solid #{current_group.primary_dark}"}
      .footer

        .right
          %button.inner-button{:onclick=>"window.location=\'#{manage_properties_path}\'",:style=>"width:200px;"}
            = "Settings"
        .right
          - if current_group.has_quick_create

            = render :partial => "shared/quick_create_button"
      .clear

.footer-mobile{:style=>"width:480px;padding:16px 0;margin:0 16px;font-size:0.8em;border-top:0px solid #{current_group.secondary}"}
  .footer
    .left{:style=>"width:50%; font-weight:bold;line-height:40px;letter-spacing:-1px;"}
      = "Every business needs a mobile FAQ : <a>Why?</a>"
    .right
      %button.inner-button{:onclick=>"window.location=\'#{new_group_path}/?mobile=true\'",:style=>"width:200px;"}
        = "Get y<u>our</u> mobile FAQ"
  .clear



















